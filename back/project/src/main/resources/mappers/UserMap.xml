<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.ict.project.login.dao.UserDao">
    <!-- 이름 확인 -->
    <insert id="getUserData" parameterType="string">
        select role from members wher id =#{id}
    </insert>
</mapper>